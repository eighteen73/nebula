pre-commit:
  parallel: true
  jobs:
    - name: css
      run: stylelint {staged_files} --fix
      glob: "**/*.scss"
      stage_fixed: true

    - name: js
      run: eslint {staged_files} --fix
      glob:
        - "**/*.js"
        - "**/*.jsx"
      stage_fixed: true

    - name: php
      run: composer run format {staged_files}
      glob: "**/*.php"
      stage_fixed: true
